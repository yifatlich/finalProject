<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Weather Result</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/weather.css">
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h1 class="mt-5">Weather Information for <%= weatherData.location.name %></h1>
            <p class="centered"><strong>Temperature:</strong> <%= weatherData.current.temp_c %> °C</p>
            <p class="centered"><strong>Condition:</strong> <%= weatherData.current.condition.text %></p>
            <p class="centered"><strong>Humidity:</strong> <%= weatherData.current.humidity %> %</p>
            <p class="centered"><strong>Wind Speed:</strong> <%= weatherData.current.wind_kph %> kph</p>
            <div class="container centered">
                <a href="/weather" class="btn btn-primary">Check Another Location</a>
            </div>
        </div>
    </div>
</body>
</html>

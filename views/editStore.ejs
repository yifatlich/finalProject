<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add Store</title>
    <link rel="stylesheet" href="/css/store.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h1 class="mt-5">Edit Store</h1>
            <form method="POST" action="/stores/edit/<%= store._id %>">
                <div class="form-group">
                    <label for="name">Store Name</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= store.name %>" required>
                </div>

                <div class="form-group">
                    <label for="address">Store Address</label>
                    <input type="text" class="form-control" id="address" name="address" value="<%= store.address %>" required>
                </div>

                <div class="form-group">
                    <label for="outlet">Outlet:</label>
                    <div>
                        <label class="radio-inline">
                            <input type="radio" name="outlet" value="yes" <%= store.outlet ? 'checked' : '' %>> Yes
                        </label>
                        <label class="radio-inline">
                            <input type="radio" name="outlet" value="no" <%= !store.outlet ? 'checked' : '' %>> No
                        </label>
                    </div>
                </div>

                <div class="form-group">
                    <label for="accessible">Accessible:</label>
                    <div>
                        <label class="radio-inline">
                            <input type="radio" name="accessible" value="yes" <%= store.accessible ? 'checked' : '' %>> Yes
                        </label>
                        <label class="radio-inline">
                            <input type="radio" name="accessible" value="no" <%= !store.accessible ? 'checked' : '' %>> No
                        </label>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Update Store</button>
            </form>
        </div>
    </div>
</body>
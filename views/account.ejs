<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accuont</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h2 class="text-center">Accuont Info</h2> 
            <table class="table table-bordered table-striped table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Phone</th>
                        <th>Street</th>
                        <th>City</th>
                        <th>State</th>
                        <th>ZipCode</th>
                        <th>CreatedAt</th>
                    </tr>
                </thead>
                <tbody>
                    <tr> 
                        <td><%= customer.username %></td>
                        <td><%= customer.email %></td>
                        <td><%= customer.phone || 'N/A' %></td>
                        <td><%= customer.address.street %></td>
                        <td><%= customer.address.city %></td>
                        <td><%= customer.address.state %></td>
                        <td><%= customer.address.zipCode %></td>
                        <td><%= customer.createdAt.toDateString() %></td>
                    </tr>
                </tbody>
            </table>

            <h2 class="text-center">Shoping History</h2>
            
            <table class="table table-bordered table-striped table-hover">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Buy date</th>
                    </tr>
                </thead>
                <tbody>
                    <% history.carts.forEach((item, index) => { %>
                        <tr>
                            <th scope="row"><%= index + 1 %></th>
                            <td>
                                <a href="/account/:username/<%=item._id %>" class="btn btn-secondary mt-4"><%= item.updatedAt %></a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
    </div>
     
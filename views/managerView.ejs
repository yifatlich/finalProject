<!DOCTYPE html>
<html lang="en">
<head>
    <link href="/css/managerView.css" rel="stylesheet" type="text/css">
</head>
<body>
    
    
    <div class="container my-5 px-3">
        <h1 class="text-center mb-4">Manager Dashboard</h1>
        
        <div class="card mb-4">
            <div class="card-header text-white">
                <h2 class="h5 mb-0">Products</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">Product Name</th>
                            <th scope="col">Quantity</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% products.forEach(product => { %>
                            <tr>
                                <td><%= product.name %></td>
                                <td><%= product.quantity %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
                <div style="text-align: right;">
                    <form action="/products" method="GET" style="display:inline;">
                        <button type="submit" class="btn btn-lg">more products details</button>
                    </form>
                </div>
            </div>
        </div>
        

        <div class="card mb-4">
            <div class="card-header  text-white">
                <h2 class="h5 mb-0">Customers</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">Username</th>
                            <th scope="col">Email</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% customers.forEach(customer => { %>
                            <tr>
                                <td><%= customer.username %></td>
                                <td><%= customer.email %></td>
                            </tr>
                        <% }) %>
                    </tbody>                    
                </table>
                <div style="text-align: right;">
                    <form action="/customers" method="GET" style="display:inline;">
                        <button type="submit" class="btn btn-lg">more customers detailes</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="card mb-4">
            <div class="card-header  text-white">
                <h2 class="h5 mb-0">Stores</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">StoreName</th>
                            <th scope="col">Address</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% stores.forEach(store => { %>
                            <tr>
                                <td><%= store.name %></td>
                                <td><%= store.address %></td>
                            </tr>
                        <% }) %>
                    </tbody>                    
                </table>
                <div style="text-align: right;">
                    <form action="/stores" method="GET" style="display:inline;">
                        <button type="submit" class="btn btn-lg">more stores detailes</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="card mb-4">
            <div class="card-header  text-white">
                <h2 class="h5 mb-0">Managers</h2>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">Manager</th>
                            <th scope="col">createdAt</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% managers.forEach(manager => { %>
                            <tr>
                                <td><%= manager.username %></td>
                                <td><%= manager.createdAt %></td>
                            </tr>
                        <% }) %>
                    </tbody>                    
                </table>
                <div style="text-align: right;">
                    <form action="/managers" method="GET" style="display:inline;">
                        <button type="submit" class="btn btn-lg">more managers detailes</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col text-center">
            <form action="/statistics" method="GET" style="display:inline;">
                    <button type="submit" class="btn btn-lg">Go to Store Satistics</button>
            </form>
        </div>
        <div class="col text-center">
            <form action="/twitter/config" method="GET" style="display:inline;">
                    <button type="submit" class="btn btn-lg">Go to twitter configs</button>
            </form>
        </div>
    </div>


    
</body>
</html>
